#!/bin/sh
set -e
# Automatically added by dh_installinit/13.31ubuntu1
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/platform-modules-s6000" ] ; then
	invoke-rc.d --skip-systemd-native platform-modules-s6000 stop || exit 1
fi
# End automatically added section

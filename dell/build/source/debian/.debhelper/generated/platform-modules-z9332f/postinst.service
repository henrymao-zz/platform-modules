# Automatically added by dh_installinit/13.31ubuntu1
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
	if [ -z "$DPKG_ROOT" ] && [ -x "/etc/init.d/platform-modules-z9332f" ]; then
		update-rc.d platform-modules-z9332f defaults >/dev/null
		if [ -n "$2" ]; then
			_dh_action=restart
		else
			_dh_action=start
		fi
		invoke-rc.d --skip-systemd-native platform-modules-z9332f $_dh_action || exit 1
	fi
fi
# End automatically added section
